<Window x:Class="FaceRecognation.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Đăng nhập" Height="300" Width="480"
    WindowStartupLocation="CenterOwner"
    WindowState="Normal"
        FontFamily="Segoe UI">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Navbar with Home button only; title moved into content area per request -->
        <Grid Grid.Row="0" Background="#2C3E50" Height="64">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Home button -->
            <Button x:Name="HomeButton" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="12,10,12,10" Width="120" Height="40" Click="HomeButton_Click"
                    Background="#FF4C6A" Foreground="White" FontWeight="Bold" Padding="8,4" ToolTip="Về trang chính">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="🏠" FontSize="16" Margin="0,0,8,0" />
                    <TextBlock Text="Home" FontWeight="Bold" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </Grid>

        <!-- Content area: title placed in white area above inputs -->
        <Grid Grid.Row="1">
            <StackPanel Width="640" HorizontalAlignment="Center" VerticalAlignment="Center">
                <!-- Moved title here -->
                <StackPanel HorizontalAlignment="Center" Margin="0,0,0,18">
                    <TextBlock Text="Cầu Lông 365:" Foreground="#2C3E50" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" />
                    <TextBlock Text="Đăng nhập tài khoản admin" Foreground="#6E7F89" FontSize="14" HorizontalAlignment="Center" />
                </StackPanel>

                <StackPanel Width="600" HorizontalAlignment="Center">
                    <TextBlock Text="Email" FontWeight="SemiBold" Margin="0,0,0,6" FontSize="16" />
                    <TextBox x:Name="EmailTextBox" Height="36" Width="600" Margin="0,0,0,12" FontSize="16" />

                    <TextBlock Text="Mật khẩu" FontWeight="SemiBold" Margin="0,0,0,6" FontSize="16" />
                    <PasswordBox x:Name="PasswordBox" Height="36" Width="600" Margin="0,0,0,18" FontSize="16" />

                    <Button x:Name="LoginButton" Content="Đăng nhập" Width="160" Height="40" HorizontalAlignment="Center" Background="LightBlue" Click="LoginButton_Click" FontSize="14" />

                    <TextBlock x:Name="StatusText" Foreground="#2C3E50" FontWeight="SemiBold" Margin="0,12,0,0" HorizontalAlignment="Center" FontSize="14" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
